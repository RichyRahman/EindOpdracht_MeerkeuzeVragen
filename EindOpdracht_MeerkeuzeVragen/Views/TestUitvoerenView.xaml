<Window x:Class="MeerkeuzeVragenApp.UI.Views.TestUitvoerenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MeerkeuzeVragenApp.UI.Views"
        mc:Ignorable="d"
        Title="Test Uitvoeren" Height="650" Width="750"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Test Uitvoeren" FontSize="20"
                   FontWeight="Bold" Margin="0,0,0,20"/>

        <TabControl Grid.Row="1">

            <!-- TAB 1: Interactief -->
            <TabItem Header="Interactief">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Test + gebruiker kiezen -->
                    <GroupBox Grid.Row="0" Header="Instellingen" Padding="10" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Test:"
                                       VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <ComboBox x:Name="CmbTest" Grid.Row="0" Grid.Column="1"
                                      Height="30" Margin="0,0,10,8"/>

                            <TextBlock Grid.Row="0" Grid.Column="2" Text="Gebruikersnaam:"
                                       VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBox x:Name="TxtGebruiker" Grid.Row="0" Grid.Column="3"
                                     Padding="5" Height="30" Margin="0,0,0,8"/>

                            <Button Grid.Row="1" Grid.Column="1" Content="▶️ Start Test"
                                    Height="35" HorizontalAlignment="Left" Width="150"
                                    Background="#2196F3" Foreground="White"
                                    Click="BtnStartTest_Click"/>
                        </Grid>
                    </GroupBox>

                    <!-- Vraag weergave -->
                    <GroupBox x:Name="GrpVraag" Grid.Row="1" Header="Vraag" 
                              Padding="10" Margin="0,0,0,10" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock x:Name="TxtVraagNummer" FontWeight="Bold"
                                       Margin="0,0,0,5" FontSize="13"/>
                            <TextBlock x:Name="TxtVraagTekst" TextWrapping="Wrap"
                                       FontSize="14" Margin="0,0,0,15"/>
                            <RadioButton x:Name="RbA" GroupName="Antwoord" Margin="0,0,0,8" FontSize="13"/>
                            <RadioButton x:Name="RbB" GroupName="Antwoord" Margin="0,0,0,8" FontSize="13"/>
                            <RadioButton x:Name="RbC" GroupName="Antwoord" Margin="0,0,0,8" FontSize="13"/>
                            <RadioButton x:Name="RbD" GroupName="Antwoord" Margin="0,0,0,8" FontSize="13"/>
                            <RadioButton x:Name="RbE" GroupName="Antwoord" Margin="0,0,0,8" 
                                         FontSize="13" Visibility="Collapsed"/>
                        </StackPanel>
                    </GroupBox>

                    <!-- Resultaten -->
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock x:Name="TxtScore" FontSize="16" FontWeight="Bold"
                                       Margin="0,0,0,10" Visibility="Collapsed"/>
                            <ItemsControl x:Name="LstFeedback">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#FFF3E0" BorderBrush="#FFB74D"
                                                BorderThickness="1" CornerRadius="4"
                                                Margin="0,0,0,8" Padding="10">
                                            <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Navigatie knoppen -->
                    <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
                        <Button x:Name="BtnVolgend" Content="Volgende ➡️" Height="38"
                                Width="150" Background="#4CAF50" Foreground="White"
                                Visibility="Collapsed" Click="BtnVolgend_Click"/>
                        <Button x:Name="BtnIndienen" Content="✅ Indienen" Height="38"
                                Width="150" Background="#FF9800" Foreground="White"
                                Visibility="Collapsed" Margin="10,0,0,0"
                                Click="BtnIndienen_Click"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- TAB 2: Bulk -->
            <TabItem Header="Bulk Verwerking">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <GroupBox Grid.Row="0" Header="Instellingen" Padding="10" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Test:"
                                       VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <ComboBox x:Name="CmbBulkTest" Grid.Row="0" Grid.Column="1"
                                      Height="30" Margin="0,0,0,8"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="CSV Bestand:"
                                       VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <Grid Grid.Row="1" Grid.Column="1" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TxtBulkPad" Grid.Column="0"
                                         IsReadOnly="True" Padding="5"
                                         VerticalContentAlignment="Center"/>
                                <Button Grid.Column="1" Content="Bladeren..." Width="90"
                                        Margin="10,0,0,0" Click="BtnBulkBladeren_Click"/>
                            </Grid>

                            <Button Grid.Row="2" Grid.Column="1" Content="⚙️ Verwerk Bulk"
                                    Height="35" HorizontalAlignment="Left" Width="160"
                                    Background="#2196F3" Foreground="White"
                                    Click="BtnVerwerkBulk_Click"/>
                        </Grid>
                    </GroupBox>

                    <!-- Bulk resultaten -->
                    <TextBlock Grid.Row="1" x:Name="TxtBulkFeedback" 
                               Margin="0,0,0,8" FontSize="12"/>
                    <DataGrid x:Name="DgBulkResultaten" Grid.Row="2"
                              AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserAddRows="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="GebruikerID" 
                                                Binding="{Binding GebruikerID}" Width="120"/>
                            <DataGridTextColumn Header="Score" 
                                                Binding="{Binding Score}" Width="80"/>
                            <DataGridTextColumn Header="Resultaat" 
                                                Binding="{Binding Resultaat}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                </Grid>
            </TabItem>
        </TabControl>

        <Button Grid.Row="2" Content="Sluiten" Height="35" Width="100"
                HorizontalAlignment="Left" Margin="0,10,0,0"
                Click="BtnSluiten_Click"/>
    </Grid>
</Window>
